import { LanguageClient } from "../interfaces/LanguageClient";
import { ShowMessage } from "../interfaces/ShowMessage";
/**
 * Try to gracefully shutdown the server using LSP `shutdown` and `exit` endpoints.
 * If the server is unresponsive, fall back to killing its process.
 *
 * @param client
 * @param workspace
 */
export declare function restartServer(client: LanguageClient, showMessage: ShowMessage): () => Promise<void>;
//# sourceMappingURL=restartServer.d.ts.map